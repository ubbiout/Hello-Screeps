var roleSew = {

    /** @param {Creep} creep **/
    run: function(creep) {
        
        //go between container and spawn/extentions
        
        //also repair containers and extentions if req
        
    }
};

module.exports = roleSew;